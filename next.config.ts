import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  basePath: '/Anubhuti-renewable-energy-private-limited',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
